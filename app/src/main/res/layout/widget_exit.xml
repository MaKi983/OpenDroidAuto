<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_exit_layout"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="4dp"
    android:background="@drawable/widget_border">

    <ImageButton
        android:id="@+id/widget_exit"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@null"
        android:contentDescription="@null"
        android:scaleType="fitCenter"
        android:src="@drawable/widget_logo"/>

    <TextView
        android:id="@+id/system_widget_top_text"
        android:textColor="#fff0f0f0"
        android:layout_width="wrap_content"
        android:layout_height="93dp"
        android:layout_alignRight="@id/widget_exit"
        android:layout_alignBottom="@id/widget_exit"
        android:layout_alignParentLeft="true"
        android:paddingRight="1dp"
        android:text="@string/widget_exit_label"
        android:textSize="27dp"
        android:gravity="center"/>
</RelativeLayout>
